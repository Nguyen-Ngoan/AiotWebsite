import { EditProductPageContent } from '../page';

export default function EditProductFeaturesPage() {
  return (
    <EditProductPageContent
      initialTab="features"
      showTabsHeader={false}
      pageTitle="Edit Product - Features"
    />
  );
}
